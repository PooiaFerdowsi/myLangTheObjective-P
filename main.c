#include <stdio.h>

int main(){
   char statement = 'a';
   scanf("%c",&statement);
   if(statement == 'e'){
      printf("\nThe file that you want to open it opned it writen in that it");
   }else{
        printf("your statement is not executable");
   }
   return 0;
}

