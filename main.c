#include <stdio.h>

int main(){
    char statement[];
    scanf("%s",&statement);
    if(statement == "){
            printf("Hello , World!");
    }else {
        printf("Your command does not exits");
    }
   return 0;
}
